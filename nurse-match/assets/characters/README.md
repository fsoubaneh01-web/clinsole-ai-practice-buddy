Drop final mascot artwork here as `nurse_<mood>.png` (`.svg` and `.webp` also
work): `nurse_idle`, `nurse_happy`, `nurse_celebrate`, `nurse_encourage`,
`nurse_sad`.

`NurseMascot` uses the image for any mood that has one and draws its vector
placeholder for the rest, so the character can be replaced one pose at a time.
